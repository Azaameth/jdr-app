**Issue 1**: The save/delete wiring added in `PlayerView.vue` (`handleSlotSave` / `handleSlotDelete`, lines ~87-109) — the core of subtask T016 and FR-003's cap-rejection requirement — has zero test coverage of the actual integration path. `InventorySlotModal.spec.ts` only verifies that the modal *displays* an `errorMessage` prop when one is handed to it directly; `PlayerView.spec.ts` only covers permission gating (owner/mj/non-owner/no-backend) and never opens the modal, never calls `saveBackpackItem`/`removeBackpackItem`/`saveEquipmentItem`/`removeEquipmentItem` through it, and never exercises the `if (success) closeSlotModal()` branch. Concretely, nothing in the test suite proves that:
  - a rejected save (store returns `false`, e.g. the category-full case) leaves `slotModalOpen` true and propagates `inventoryStore.error.value` into the modal's `error-message` prop, or
  - a successful save/delete actually closes the modal.

  This is exactly the scenario the WP's own "Reviewer Guidance" calls out ("Try the cap path mentally: nourriture already has 1 item, add another — store must reject, modal must show the message") and exactly what the WP's Definition of Done checkbox "Category-full rejection surfaces the French store error in the modal" is meant to certify. Right now that DoD item is only true by code inspection, not by test.

  **Fix**: add a `PlayerView.spec.ts` case (or an equivalent test at the composition point) that mounts `PlayerView` with a mocked `inventoryStore` whose `saveBackpackItem` resolves `false` and whose `error` is set to a French message, opens the modal via a slot click, submits, and asserts the modal is still open (`InventorySlotModal` still rendered/open) with that error text visible. Add a companion case where the store call resolves `true` and asserts the modal closes. This is the only place that can prove the wiring introduced by this WP (not the store, not the modal in isolation) behaves correctly.

---

All other checklist items passed with direct evidence:
- Diff scope: WP03 commit `79cc781` touches exactly `AppModal.vue`, `InventorySlotModal.vue`, their specs, `PlayerView.vue` + new spec, and the expected out-of-map `editable` prop additions to `BackpackGrid.vue`/`WeaponArmorList.vue` (documented in the commit message as expected). `useInventoryStore.ts` and `InventoryRepository.ts` are untouched (`git show 79cc781 -- src/controllers/useInventoryStore.ts src/models/repositories/InventoryRepository.ts` is empty).
- FR-003 CRUD: `handleSlotSave`/`handleSlotDelete` call `saveBackpackItem`/`removeBackpackItem`/`saveEquipmentItem`/`removeEquipmentItem` per `contracts/data-layer.md`; no repository imports in any WP03 file.
- FR-009 permissions: `canEditInventory` gates on `inventory.uid === currentUser.uid || isMj || isAdmin`, defaults to false with no inventory/no user (degraded mode). Gating is structural: `BackpackGrid.vue`/`WeaponArmorList.vue` render `<component :is="editable ? 'button' : 'div'">` — non-editors get plain `<div>`s, not disabled/hidden buttons. `PlayerView.spec.ts` asserts `tagName === 'DIV'` for non-owner joueur, mismatched-inventory-uid case, and no-backend mode, and `tagName === 'BUTTON'` for owner and mj.
- Cap enforcement: lives entirely in `useInventoryStore.ts` (`CATEGORY_FULL_ERROR`, unmodified by this WP) — WP03 only reads `inventoryStore.error.value`, does not reimplement cap logic. (Test coverage of the modal-stays-open behavior is the subject of Issue 1 above.)
- AppModal genericity: no inventory-specific logic in `AppModal.vue`; accessibility basics present (`role="dialog"`, `aria-modal="true"`, Escape close, backdrop-vs-content click distinction, ✕ button, focus moved into panel on open) and covered by `AppModal.spec.ts`.
- Lossless stat round-trip: traced manually — a weapon/armor item with `statNote` only (e.g. `{ name: 'X', statNote: 'vs proj. magiques' }`) pre-fills via `formatWeaponArmorStat` → `'vs proj. magiques'`, and on save `parseWeaponArmorText('X (vs proj. magiques)')` returns `{ name: 'X', statNote: 'vs proj. magiques' }` — lossless. The underlying primitives are well covered by `inventoryText.spec.ts` (WP01), including this exact `vs proj. magiques` case; `InventorySlotModal.spec.ts` doesn't add a dedicated statNote-only round-trip case, but given the manual trace and existing primitive coverage this is a minor observation, not a blocker.
- French strings: consistent tone with the rest of the app.
- WP02 regression: `editable` prop defaults to `true` in both `BackpackGrid.vue` and `WeaponArmorList.vue`; WP03 commit does not touch `BackpackGrid.spec.ts`/`WeaponArmorList.spec.ts`, and all 167 unit tests (including those) pass.
- Gates: `npm run type-check` clean, `npm run lint` clean (oxlint + eslint, no `--fix` diffs left uncommitted), `npm run test:unit` → 18 files / 167 tests passed.
- Anti-pattern checklist: dead code — none (both `AppModal` and `InventorySlotModal` have live production callers); frozen surface — respected; locked decision (store-not-repository) — respected; shared-file ownership — recorded in the commit message per the WP's own out-of-map note; production fragility — no new `throw`/silent-swallow patterns introduced.
